I"Ï<p>The best way to try out OpenSearch is to use <a href="https://docs.docker.com/compose/install/">Docker Compose</a>. These steps will setup a two node cluster of OpenSearch plus OpenSearch Dashboards:</p>
:ET